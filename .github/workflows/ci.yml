name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Upload to Sharepoint
      uses: INVIXO/actions-sharepoint-upload@beta
      with:
       site_url: '1url111'
       client_id: '1sdfsdfclient_id1'
       client_secret: '1client_secret1'
       glob: '1glob1'
