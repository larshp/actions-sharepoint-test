name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Upload to Sharepoint
      uses: INVIXO/actions-sharepoint-upload@beta
      with:
       site_url: '1urlsdfsdfsdf111'
       client_id: '1client11'
       client_secret: '1cliensdft_sesdfcret1'
       relative_url: '1relativsdfe1'
       glob: '*.*'
